#
#   Foxely: fibonacci
#   https://github.com/Foxely/Foxely
#

import "time";

fibonacci :: func(n)
{
    if (n < 2) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

start := clock();

for (i := 0; i < 5; ++i)
{
    print "%\n", fibonacci(28);
}