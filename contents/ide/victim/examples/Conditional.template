{-
    control flow implementation of Victim
-}


var age := 42;

if (age < 14)
    print( "don't go to highschool!" );
else if (age < 18)
    print( "stay in school and don't get pregnant!" );
else
    print( "get the hell out of highschool!" );


-- multi line if else statement

var a := 21;
var b := a;

var op := null;
op := "add";

if (op == "add")
{
    var res := a + b;
    print(res);
}
else
{
    print("unknown operation!");
}
